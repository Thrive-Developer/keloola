{
  "name": "@repo/support",
  "version": "0.0.0",
  "devDependencies": {
    "@types/node": "^25.2.0",
    "prettier": "^3.7.4"
  },
  "bin": {
    "generate-env": "./generate-env.ts"
  },
  "license": "MIT",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "format": "prettier --write \"**/*.ts\" --ignore-path ../.prettierignore",
    "format:check": "prettier --check \"**/*.ts\" --ignore-path ../.prettierignore",
    "check-types": "tsc --noEmit",
    "lint": "echo 'No lint configured for support package'"
  },
  "peerDependencies": {
    "n8n-core": "*",
    "n8n-workflow": "*"
  }
}
